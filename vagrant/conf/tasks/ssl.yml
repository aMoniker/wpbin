- name: ssl | create self-signed key/cert
  command: openssl req -new -nodes -x509 -subj "/C=US/ST=New York/L=/O=/CN={{ site_name }}" -days 3650 -keyout /etc/ssl/{{ site_name }}.key -out /etc/ssl/{{ site_name }}.crt -extensions v3_ca creates=/etc/ssl/{{ site_name }}.crt